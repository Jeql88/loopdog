{
  "name": "loopdog",
  "version": "0.0.0",
  "description": "Drop an AI-engineering workflow into any repo and run its autonomous implementation loop.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "loopdog": "dist/cli.js"
  },
  "files": [
    "dist",
    "templates",
    "ralph"
  ],
  "scripts": {
    "build": "tsc",
    "test": "tsx --test test/**/*.test.ts",
    "loopdog": "tsx src/cli.ts",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
