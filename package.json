{
  "name": "loopdog",
  "version": "0.0.0",
  "description": "Drop a Matt-Pocock-style AI-engineering workflow into any repo and run it autonomously.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "loopdog": "dist/cli.js"
  },
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsc",
    "test": "tsx --test test/**/*.test.ts",
    "loopdog": "tsx src/cli.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
